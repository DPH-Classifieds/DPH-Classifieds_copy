[phases.setup]
nixPkgs = ["python311Full", "gcc"]

[phases.install]
cmds = [
  "python -m venv .venv && . .venv/bin/activate && pip install --upgrade pip setuptools wheel",
  ". .venv/bin/activate && pip install -r requirements.txt",
]

[start]
cmd = "sh -c 'export PATH=\"/root/.nix-profile/bin:/nix/var/nix/profiles/default/bin:$(pwd)/.venv/bin:$PATH\"; if [ \"${SERVICE_ROLE:-web}\" = \"worker\" ]; then exec python worker.py; else exec gunicorn -c gunicorn.conf.py app:app; fi'"
