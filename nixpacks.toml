[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["cd flask-react-supabase-app/backend && pip install -r requirements.txt"]

[start]
cmd = "cd flask-react-supabase-app/backend && gunicorn app:app --bind 0.0.0.0:${PORT} --workers 2 --timeout 120"
