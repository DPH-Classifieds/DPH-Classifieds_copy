{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "python -m venv .venv && . .venv/bin/activate && pip install -r flask-react-supabase-app/backend/requirements.txt"
  },
  "deploy": {
    "healthcheckPath": "/api/health",
    "startCommand": "export PATH=\"$(pwd)/.venv/bin:$PATH\"; if [ \"${SERVICE_ROLE:-web}\" = \"worker\" ]; then python flask-react-supabase-app/backend/worker.py; else gunicorn -c flask-react-supabase-app/backend/gunicorn.conf.py --chdir flask-react-supabase-app/backend app:app; fi",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
