import React from 'react';
import { OTPVerification } from './ui/otp-verify';

const PhoneVerificationFlow = (props) => {
  return <OTPVerification {...props} />;
};

export default PhoneVerificationFlow;
