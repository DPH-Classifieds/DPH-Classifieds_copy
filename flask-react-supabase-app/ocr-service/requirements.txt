fastapi==0.141.1
# Keep the Starlette security fixes explicit; FastAPI 0.141.1 accepts Starlette >=0.46.
starlette==1.6.0
uvicorn[standard]==0.34.0
gunicorn==26.2.0
python-multipart==0.0.32
pillow==12.3.0
numpy==1.26.4
paddlepaddle==2.6.2
paddleocr==2.8.1
# Keep the OpenCV runtime explicit and patched; this service does not use any
# contrib-only modules, so do not add the larger opencv-contrib distribution.
opencv-python==4.11.0.86
